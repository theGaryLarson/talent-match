
BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[job_postings] ALTER COLUMN [job_description] TEXT NOT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH

BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[events] ADD [recordingLink] VARCHAR(2083);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH

BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[RAGRecordManager] ALTER COLUMN [hash] NVARCHAR(64) NOT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH

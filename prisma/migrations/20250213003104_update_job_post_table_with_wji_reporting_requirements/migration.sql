BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[job_postings] ADD [earn_and_learn_type] VARCHAR(255),
[occupation_code] VARCHAR(255);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH

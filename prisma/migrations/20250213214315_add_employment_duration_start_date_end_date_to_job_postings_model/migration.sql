BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[job_postings] ADD [employment_duration] VARCHAR(255),
[end_date] DATETIME,
[start_date] DATETIME;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
